Steps to patch and rebuild ubic website:

1. hack on templates in src/
2. make local; open index.html in browser
3. goto 1 until done
2. make     # regenerate html with correct <base> tag
3. git push
